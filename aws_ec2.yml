plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  "tag:Owner": "yoav_target*" 
keyed_groups:
  - key: tags.Name
    prefix: name      
  - key: tags.Service
    prefix: service   
  - key: tags.Restart
    prefix: restart  
compose:
  ansible_host: public_ip_address
